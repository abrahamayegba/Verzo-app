mutation ResetPassword(
  $userId: String!
  $oldPassword: String!
  $newPassword: String!
) {
  resetPassword(
    userId: $userId
    input: { oldPassword: $oldPassword, newPassword: $newPassword }
  )
}
