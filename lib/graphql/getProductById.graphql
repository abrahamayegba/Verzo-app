query GetProductById($productId: String!) {
  getProductById(productId: $productId) {
    productName
    price
    productUnit {
      unitName
    }
    businessProductUnit {
      unitName
      id
    }
    productUnitId
    reorderLevel
    trackReorderLevel
  }
}
