query GetDailyExpensesForMonth(
  $businessId: String!
  $month: Int!
  $year: Int!
) {
  getDailyExpensesForMonth(
    businessId: $businessId
    month: $month
    year: $year
  ) {
    expenseDate
    totalExpenses
  }
}
